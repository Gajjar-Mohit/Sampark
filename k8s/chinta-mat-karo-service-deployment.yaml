apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml --out k8s/
    kompose.version: 1.37.0 (HEAD)
  labels:
    io.kompose.service: chinta-mat-karo-service
  name: chinta-mat-karo-service
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: chinta-mat-karo-service
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml --out k8s/
        kompose.version: 1.37.0 (HEAD)
      labels:
        io.kompose.service: chinta-mat-karo-service
    spec:
      containers:
        - args:
            - sh
            - -c
            - |2
                echo 'Waiting for database to be ready...' &&
                bun prisma migrate deploy &&
                bun run seed &&
                bun run src/index.ts
          env:
            - name: DATABASE_URL
              value: postgresql://chinta_mat_karo_user:chinta_mat_karo_password@chinta-mat-karo-db:5432/chinta_mat_karo_db
            - name: IIN
              value: "456123"
            - name: KAFKA_BASEURL
              value: kafka:9092
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3000"
            - name: REDIS_DB
              value: "0"
            - name: REDIS_HOST
              value: chinta-mat-karo-redis-stack
            - name: REDIS_PASSWORD
            - name: REDIS_PORT
              value: "6379"
            - name: REDIS_URL
              value: redis://chinta-mat-karo-redis-stack:6379
          image: chinta-mat-karo-service
          name: chinta-mat-karo-service
          ports:
            - containerPort: 3000
              protocol: TCP
      restartPolicy: Always

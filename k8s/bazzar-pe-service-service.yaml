apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml --out k8s/
    kompose.version: 1.37.0 (HEAD)
  labels:
    io.kompose.service: bazzar-pe-service
  name: bazzar-pe-service
spec:
  ports:
    - name: "6001"
      port: 6001
      targetPort: 8000
  selector:
    io.kompose.service: bazzar-pe-service
